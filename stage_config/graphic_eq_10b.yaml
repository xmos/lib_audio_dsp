module:
  graphic_eq_10b:
    gains:
      type: int32_t
      size: 10
      help: >
        The graphic EQ gains in Q29 format. To convert a value in decibels to
        this format, the function ``db_to_qxx`` in ``control/helpers.h``.
includes:
  - "stdint.h"
