module:
  graphic_eq_10b:
    gains:
      type: int32_t
      size: 10
      help: >
        The graphic EQ gains in Q31 format. To convert a value in decibels to
        this format, the function :c:func:`geq_db_to_gain` in ``control/helpers.h``.
includes:
  - "stdint.h"
