module:
  dsp_thread:
    max_cycles:
      type: uint32_t
      help: >
        The maximum number of clock cycles consumed by a DSP thread.
        To avoid poor audio, this must be less than the number of
        clock cycles available to the thread.
includes: []
