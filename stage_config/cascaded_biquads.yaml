module:
  cascaded_biquads:
    left_shift:
      type: int
      size: 8
      help: The coefficient shift applied to each biquad in the cascade.
    filter_coeffs:
      type: int32_t
      size: 40
      attribute: DWORD_ALIGNED
      help: The normalised biquad filter coefficients for each biquad in the cascade as an array of [5][8].
includes:
  - "stdint.h"
  - "xmath/api.h"
